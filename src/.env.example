# No environment variables required for Phase 1.
# Add variables here with placeholder values when new integrations are added.
# Copy this file to .env.local and fill in real values — never commit .env.local.
